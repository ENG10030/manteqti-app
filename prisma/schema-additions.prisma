// ============================================
// أضف هذه الحقول إلى نموذج Apartment
// في ملف prisma/schema.prisma
// ============================================

model Apartment {
  // ... الحقول الموجودة ...

  // حقول VIP والمميز - أضف هذه الأسطر
  isFeatured   Boolean    @default(false)   // عقار مميز (يظهر أولاً)
  isVip        Boolean    @default(false)   // عقار VIP (شارة ذهبية)
  featuredUntil DateTime?                    // تاريخ انتهاء التمييز

  // ... باقي الحقول ...
}

// ============================================
// بعد الإضافة، نفذ هذه الأوامر:
// npx prisma db push
// npx prisma generate
// ============================================
